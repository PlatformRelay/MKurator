apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- bases/messaging.mkurator.dev_authorityrecords.yaml
- bases/messaging.mkurator.dev_channelauthrules.yaml
- bases/messaging.mkurator.dev_channels.yaml
- bases/messaging.mkurator.dev_queuemanagerconnections.yaml
- bases/messaging.mkurator.dev_queues.yaml
- bases/messaging.mkurator.dev_topics.yaml

# The CRDs are single-version (v1beta1) with no conversion webhook, so the
# conversion webhook_in_*/cainjection_in_*
# patches and their kustomizeconfig wiring have been removed.
