sonar.projectKey=PlatformRelay_MKurator
sonar.organization=platformrelay
sonar.projectName=MKurator
sonar.sources=.
sonar.exclusions=**/zz_generated.*,**/vendor/**,**/charts/**,**/hack/**,**/config/**,**/test/**,**/*_test.go
sonar.tests=.
sonar.test.inclusions=**/*_test.go
sonar.go.coverage.reportPaths=coverage.out
sonar.coverage.exclusions=**/api/**,**/cmd/**,**/test/**

# Accepted-safe suppression (config-as-code; keeps the SonarCloud dashboard honest).
# go:S2245 on drift_resync.go: math/rand is intentional, non-cryptographic requeue jitter for
# control-plane drift resync scheduling — no token/secret material. Mirrors the in-tree
# //nolint:gosec G404 rationale (REQ-SONAR-MK-01). Not crypto/rand by design.
sonar.issue.ignore.multicriteria=driftRand
sonar.issue.ignore.multicriteria.driftRand.ruleKey=go:S2245
sonar.issue.ignore.multicriteria.driftRand.resourceKey=**/internal/controller/drift_resync.go
