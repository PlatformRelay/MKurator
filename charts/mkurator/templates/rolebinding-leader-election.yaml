{{- if and .Values.rbac.create .Values.leaderElection.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "mkurator.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mkurator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "mkurator.fullname" . }}-leader-election
subjects:
  - kind: ServiceAccount
    name: {{ include "mkurator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
