{{- if and .Values.webhooks.enabled .Values.webhooks.certManager.create }}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "mkurator.selfSignedIssuerName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mkurator.labels" . | nindent 4 }}
    app.kubernetes.io/component: certificate
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "mkurator.servingCertName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mkurator.labels" . | nindent 4 }}
    app.kubernetes.io/component: certificate
spec:
  dnsNames:
    {{- $svc := include "mkurator.webhookServiceName" . }}
    - {{ printf "%s.%s.svc" $svc .Release.Namespace }}
    - {{ printf "%s.%s.svc.cluster.local" $svc .Release.Namespace }}
  issuerRef:
    kind: Issuer
    name: {{ include "mkurator.selfSignedIssuerName" . }}
  secretName: {{ .Values.webhooks.certManager.secretName }}
{{- end }}
